# ───────────────────────────────────────────────────────────────
# CONTEXT & REQUEST: Cookie Consent Issues
# ───────────────────────────────────────────────────────────────

context: |
  Environment: /cookie_consent
  State: Cookie consent banner and modal with multiple functionality issues | Stack: Webflow, JavaScript (modal_welcome.js), Finsweet | Dependencies: Webflow positioning, modal_welcome.js, Finsweet components, focus trap
  Constraints: All positioning in Webflow, must maintain modal_welcome.js pattern | Related: Banner (mobile), Modal (desktop), background handlers, focus trap

request: |
  Objective: Fix cookie consent banner and modal across mobile/desktop | Type: bug | Scope: Mobile visibility, modal positioning, background close, focus trap
  Requirements: Functional[Banner shows on mobile, modal centers, closes on BG click, focus trap triggers] Technical[Remove positioning overrides, switch to BG attribute for close, match modal_welcome.js focus trap] UX[Modal fixed center, focus trap per screenshot, consistent trigger]
  Success: Banner visible mobile, modal centered all screens, BG click closes, focus trap correct appearance and triggers | Testing: Mobile devices, positioning verification, BG click, focus trap visual match and trigger reliability
3